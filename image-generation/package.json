{
  "name": "image-generation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export NODE_OPTIONS=--openssl-legacy-provider; ./node_modules/.bin/netlify-lambda serve src --config webpack.config.js",
    "build": "unset NODE_OPTIONS&&export NODE_OPTIONS=--openssl-legacy-provider&&./node_modules/.bin/netlify-lambda build src --config webpack.config.js"
  },
  "author": "Borwornpob Thumrongchotikhun",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.10.2",
    "express": "^4.18.2",
    "netlify-lambda": "^2.0.15",
    "qrcode": "^1.5.1",
    "serverless-http": "^3.1.0",
    "webpack-node-externals": "^3.0.0"
  }
}
